// Setup global pour Vitest. Pour l'instant on n'a rien a configurer
// (jsdom est setup via vite.config.ts environment). On garde le fichier
// pour pouvoir y brancher plus tard testing-library/jest-dom matchers
// ou des mocks globaux.
export {};
