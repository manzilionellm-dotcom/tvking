# QA — Run 001 (2026-07-23)

Séquence complète exécutée localement (Node 22). Vert = exit 0.

| Gate | Commande | Exit | Détail |
|---|---|---|---|
| Lint | `npm run lint` | 0 | 0 erreur / 0 warning |
| Types | `npm run typecheck` | 0 | `tsc --noEmit` |
| Tests | `npm run test:coverage` | 0 | 40/40 · couv. 100% lignes, 98.9% branches (seuils 80/70) |
| Build | `npm run build` | 0 | 115 pages statiques |
| Budget | `node scripts/size-budget.mjs` | 0 | 647.7 KiB / budget 712.5 KiB (+0.0%) |

Revue du diff :
- Migration de stockage : néant (aucune modif du schéma localStorage `tvking:*`).
- Contrat d'API : néant (app mock, pas de back-end consommé).
- Permissions : inchangées.
- Conformité : inchangée.
- Nouvelle collecte de données : NON — telemetry est local (ring en mémoire), scrubbé, aucun sink réseau par défaut.

Verdict : ACCEPTÉ.

## Matrice d'appareils — test manuel (physique, non pilotable par l'agent)
À exécuter sur une vraie box TV / télécommande D-pad (≈5 min) :
1. Naviguer une rangée à droite en plein milieu, ouvrir un titre, presser RETOUR → le focus revient sur LA MÊME carte (restauration).
2. Dans le lecteur : touche Play/Pause de la télécommande → bascule ; Stop → retour à la fiche.
3. RETOUR global depuis n'importe quel écran (hors accueil) → recule d'un cran, jamais de cul-de-sac.
4. Focus toujours visible (anneau doré + agrandissement) à chaque déplacement.
