import { Preferences } from "../components/Preferences";

// Page RÉGLAGES : profils + accessibilité + modes (repos des yeux, VIP).
export default function ReglagesPage() {
  return <Preferences />;
}
